export const appConfig = {
    //Configurar API: Almacena URLs base de APIs, claves de API, y otros parámetros relacionados con las APIs
    apiBaseUrl: 'https://api.example.com',
    apiKey: 'your-api-key',
    // Configurar Entorno: Almacena configuraciones específicas de cada entorno (desarrollo, pruebas, producción).
    environment: 'development',
    debugMode: true,
    // Declarar constantes que se utilizan en toda la aplicación, como límites de paginación, mensajes de error, etc.
    paginationLimit: 10,
    defaultErrorMessage: 'Something went wrong. Please try again later.',
    //Configurar estilos o temas
    theme: {
      primaryColor: '#0078d4',
      secondaryColor: '#ffffff',
    },
  };
  
  
    